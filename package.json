{
  "name": "mdl",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "build": "tsc -b",
    "dev": "sh -c 'pnpm -s build && while [ \"$1\" = \"--\" ]; do shift; done && node packages/cli/dist/index.js \"$@\"' --",
    "mdl": "sh -c 'pnpm -s build && while [ \"$1\" = \"--\" ]; do shift; done && node packages/cli/dist/index.js \"$@\"' --",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run"
  },
  "devDependencies": {
    "@jannchie/eslint-config": "^3.7.8",
    "@types/node": "^24.6.0",
    "eslint": "^10.2.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
